func mid(px, py) {
    vars nx = px
    vars ny = py + 1
    return inner(0, nx, ny)
}
func inner(type, px, py) {
    vars by = py
    vars row = 1
    if (by + row >= 20) { return 0 }
    return by
}
vars rr = mid(3, 18)
display "rr={rr}"
display "s4-done"