func mid(px, py) {
    vars nx = px
    vars ny = py + 1
    return inner(0, nx, ny)
}
func inner(type, px, py) {
    vars by = py
    return by
}
vars rr = mid(3, 18)
display "rr={rr}"
display "s2-done"